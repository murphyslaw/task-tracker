{
  "tasks": {
    "coverage": "rm -rf coverage/ && deno test --coverage && deno coverage --lcov --output=coverage/cov.lcov coverage/ && genhtml -o coverage/html coverage/cov.lcov && open coverage/html/index.html"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1",
    "@std/expect": "jsr:@std/expect@^1.0.6",
    "@std/testing": "jsr:@std/testing@^1.0.3"
  }
}
